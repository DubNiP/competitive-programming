#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int h,a,b;
    cin>>h>>a>>b;
    if(h>=a&&h<=b) cout<<"SIM\n";
    else cout<<"NAO\n";
}

int main() {
        ios_base::sync_with_stdio(0); cin.tie(0);
        int q = 1;
                    while(q--) solve();
                        return 0;
} 
